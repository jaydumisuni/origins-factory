name: Apply Phase 7 Review Fixes

on:
  push:
    branches: [build/phase7-capability-evolution]
    paths:
      - "tools/apply_phase7_review_fixes.py"
      - ".github/workflows/apply-phase7-review-fixes.yml"

permissions:
  contents: write

jobs:
  patch:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - name: Check out Phase 7 branch
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          fetch-depth: 0

      - name: Set up Python 3.12
        uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065 # v5
        with:
          python-version: "3.12"

      - name: Set up Node 24
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: "24"
          cache: npm
          cache-dependency-path: workspace/package-lock.json

      - name: Apply independent review fixes
        run: python tools/apply_phase7_review_fixes.py

      - name: Verify Python and proof tools
        run: |
          python -m py_compile \
            python/origins_integration/capability_evolution.py \
            python/origins_integration/capability_evolution_approvals.py \
            python/origins_integration/engineering.py \
            python/origins_integration/phase7_server.py \
            python/tests/test_phase7_review_hardening.py \
            tools/prove_phase7_live_owner.py \
            tools/prove_phase7_live_owner_strict.py \
            tools/prove_phase7_workspace_ui.py
          git diff --check
          python -m pip install --disable-pip-version-check -e ./python pytest
          python -m pytest -q python/tests/test_phase7_*.py

      - name: Verify Workspace
        working-directory: workspace
        run: |
          npm ci
          npm run typecheck
          npm test
          npm run build

      - name: Commit review fixes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add \
            python/origins_integration/capability_evolution.py \
            python/origins_integration/capability_evolution_approvals.py \
            python/origins_integration/engineering.py \
            python/origins_integration/phase7_server.py \
            python/tests/test_phase7_review_hardening.py \
            tools/prove_phase7_live_owner.py \
            tools/prove_phase7_live_owner_strict.py \
            tools/prove_phase7_workspace_ui.py \
            workspace/src/phase7.css
          git diff --cached --quiet && { echo "No review fixes required"; exit 0; }
          git commit -m "Phase 7: resolve independent review findings"
          git push origin HEAD:build/phase7-capability-evolution
